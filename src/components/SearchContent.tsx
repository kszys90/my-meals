/* eslint-disable react/jsx-indent */
/* eslint-disable indent */
import React from 'react'

export const SearchContent = ({ children }: { children: React.ReactNode }) => {
    return (
        <div>
            <div>
                content
            </div>
            {children}
        </div>
    )
}
